# Hermes Windows 适配代码修改总结

> 更新时间：2026-04-30
> 对比基准：分支 hermes/2026-04-29（提交 23ae0dfb）
> 变更范围：同步上游 v2026.4.30（262 个提交）、解决 4 处合并冲突、保留全部 Windows 优化

---

## 本次操作概述

从 `hermes/2026-04-29` 分支创建 `hermes/2026-04-30`，合并上游 tag `v2026.4.30`（Hermes Agent v0.12.0，The Curator release），带入 262 个新提交，解决 4 处冲突，所有 Windows 适配补丁完整保留。

---

## 合并统计

| 项目 | 数量 |
|------|------|
| 合并的上游提交数 | 262 |
| 自动合并成功文件 | 全部，除 4 个 |
| 手动解决冲突文件 | 4 |
| Windows 优化丢失 | 0 |

---

## 冲突解决详情

### 1. tools/environments/local.py（2 处冲突）

#### 冲突一：`__init__` 方法

**冲突原因：**
上游新增了 `os.path.expanduser(cwd)` 展开用户目录（`~` 路径支持）；我们在 04-24 的修改对 Windows 做了 MSYS 路径转换 + 双路径存储。

**解决方案：**
两者均保留——Windows 分支使用我们的 MSYS 转换，非 Windows 分支使用上游的 `expanduser`：

```python
def __init__(self, cwd: str = "", timeout: int = 60, env: dict = None):
    if _IS_WINDOWS:
        from tools.platform_compat import windows_path_to_msys
        init_cwd = windows_path_to_msys(cwd) if cwd else windows_path_to_msys(os.getcwd())
    else:
        init_cwd = os.path.expanduser(cwd) if cwd else os.getcwd()

    super().__init__(cwd=init_cwd, timeout=timeout, env=env)

    if _IS_WINDOWS:
        from tools.platform_compat import msys_path_to_windows
        self._snapshot_path_win = msys_path_to_windows(self._snapshot_path)
        self._cwd_file_win = msys_path_to_windows(self._cwd_file)
    else:
        self._snapshot_path_win = self._snapshot_path
        self._cwd_file_win = self._cwd_file

    self.init_session()
```

#### 冲突二：`_update_cwd` 方法

**冲突原因：**
上游将 `open(...)` 改为 `with open(...) as f:` 上下文管理器（代码质量提升）；我们的版本使用 `_cwd_file_win`（Windows 路径）。

**解决方案：**
采用上游的 context manager 形式，同时保留 `_cwd_file_win`：

```python
with open(self._cwd_file_win) as f:
    cwd_path = f.read().strip()
```

---

### 2. hermes_cli/gateway.py

**冲突原因：**
我们在 04-26 添加了 `encoding="utf-8", errors="replace"`；上游也添加了 `encoding="utf-8"`，但将 `errors` 改为 `"ignore"`。

**解决方案：**
上游已有相同的 UTF-8 编码修复，采用上游版本（`errors="ignore"`）：

```python
result = subprocess.run(
    ["wmic", "process", "get", ...],
    capture_output=True,
    text=True,
    encoding="utf-8",
    errors="ignore",
    timeout=10,
)
```

---

### 3. hermes_cli/main.py

**冲突原因：**
上游将 `main()` 函数中的 `argparse.ArgumentParser(...)` 大段内联定义提取到独立模块 `hermes_cli/_parser.py`（`build_top_level_parser()`）；我们在 04-26 在 `main()` 开头添加了 Windows stdout/stderr UTF-8 重配置块。两处修改在 `main()` 函数入口处重叠。

**解决方案：**
保留我们的 Windows UTF-8 重配置块，采用上游的 `build_top_level_parser()` 重构（丢弃大段内联 epilog）：

```python
def main():
    # Reconfigure stdout/stderr to UTF-8 on Windows so that Unicode symbols
    # (✓, ✗, →, etc.) don't crash on CJK codepages (GBK / cp949 / …).
    if sys.platform == "win32":
        for _stream in (sys.stdout, sys.stderr):
            if hasattr(_stream, "reconfigure"):
                try:
                    _stream.reconfigure(encoding="utf-8", errors="replace")
                except Exception:
                    pass

    from hermes_cli._parser import build_top_level_parser

    parser, subparsers, chat_parser = build_top_level_parser()
```

---

### 4. tui_gateway/server.py

**冲突原因：**
上游将 `session.create` 中的内联 `_build()` 线程函数提取为顶层函数 `_start_agent_build(sid, session)`，并将触发方式改为 `threading.Timer(0.05, _deferred_build)`。我们在 04-26 的 `_build` 内联函数中添加了 `ephemeral` 检查（`if db is not None and not ephemeral:`）。

上游提取的 `_start_agent_build` 函数（line 473）缺少 `ephemeral` 检查。

**解决方案：**

1. 采用上游的 Timer 触发方式（丢弃我们的内联 `_build` 重复代码）：
```python
build_timer = threading.Timer(0.05, _deferred_build)
build_timer.daemon = True
build_timer.start()
```

2. 在 `_start_agent_build` 函数的 `_build()` 内部补上 `ephemeral` 检查：
```python
db = _get_db()
if db is not None and not current.get("ephemeral"):
    db.create_session(key, source="tui", model=_resolve_model())
```

这确保 web 侧边栏创建的 ephemeral session 仍然不会写入数据库。

---

## 上游重要变更（v0.12.0 The Curator Release）

| 方向 | 说明 |
|------|------|
| Curator 自主维护 | 新增 `agent/curator.py`，Hermes 可自主运行后台 Curator 循环 |
| 新增推理提供商 | 扩展至 20+ 推理后端 |
| Microsoft Teams | 通过可插拔网关平台支持 Teams |
| Spotify / Google Meet | 新增集成 |
| ComfyUI / TouchDesigner-MCP | 默认捆绑 |
| TUI 冷启动性能 | ~57% 冷启动时间削减 |
| `_start_agent_build` 重构 | session.create 的 agent 构建逻辑提取为独立函数，Timer 延迟触发 |
| `build_top_level_parser` 重构 | CLI 参数解析器提取到 `hermes_cli/_parser.py` |

---

## Windows 优化保留确认

| Windows 补丁 | 文件 | 状态 |
|-------------|------|------|
| WSL bash 检测 (`_is_wsl_bash`) | `tools/environments/local.py` | ✅ 自动合并保留 |
| Git Bash 路径探测优先级 | `tools/environments/local.py` | ✅ 自动合并保留 |
| MSYS 路径转换 + 双路径存储 | `tools/environments/local.py` | ✅ 手动解决冲突保留（融合 expanduser） |
| Windows 管道读取 (`_drain_windows`) | `tools/environments/base.py` | ✅ 自动合并保留 |
| 跨平台辅助库 | `tools/platform_compat.py` | ✅ 自动合并保留 |
| 危险命令拦截 (`find /` 等) | `tools/approval.py` | ✅ 自动合并保留 |
| stdin/stdout/stderr UTF-8 重配置 | `tui_gateway/entry.py` | ✅ 自动合并保留 |
| StdioTransport 绕过 TextIOWrapper | `tui_gateway/transport.py` | ✅ 自动合并保留 |
| slash_worker UTF-8 全量重配置 | `tui_gateway/slash_worker.py` | ✅ 自动合并保留 |
| ephemeral session 支持 | `tui_gateway/server.py` | ✅ 手动解决冲突保留（移入 `_start_agent_build`） |
| PID 文件编码 + taskkill 编码 | `gateway/status.py` | ✅ 自动合并保留 |
| ConPTY Windows 后端 | `hermes_cli/pty_bridge.py` | ✅ 自动合并保留 |
| HERMES_PYTHON 环境变量传递 | `hermes_cli/web_server.py` | ✅ 自动合并保留 |
| npm 子进程 UTF-8 编码 | `hermes_cli/main.py` | ✅ 自动合并保留 |
| main() UTF-8 重配置 | `hermes_cli/main.py` | ✅ 手动解决冲突保留（保留在 build_top_level_parser 重构前） |
| 146 处 subprocess encoding 修复 | 41 个文件 | ✅ 自动合并保留 |

---

## 提交记录

| 提交 | 说明 |
|------|------|
| `a164f2b85` | Merge v2026.4.30 into hermes/2026-04-30, preserve Windows compat patches |

---

## 操作总结

本次 04-30 的工作为纯同步操作，无新增 Windows 适配代码（除修正上游遗漏的 `ephemeral` 检查外）。核心工作是：

1. **无损合并 262 个上游提交**：功能更新包括 Curator 自主维护循环、多新推理提供商、TUI 冷启动 57% 加速、Teams/Spotify/Google Meet 集成等
2. **解决 4 处冲突**：`local.py` 合并 expanduser + MSYS 转换；`gateway.py` 采用上游 errors="ignore"；`main.py` 保留 UTF-8 重配置并采用 parser 重构；`server.py` 采用 Timer 触发并补充 ephemeral 检查至 `_start_agent_build`
3. **全部 Windows 优化完整保留**：16 项 Windows 补丁（含 04-24、04-26、04-27、04-29 的全部修改）无一丢失

---

> 合并基准：hermes/2026-04-29（23ae0dfb）合并上游 tag v2026.4.30（73bf3ab1b）
> 合并提交：a164f2b85
